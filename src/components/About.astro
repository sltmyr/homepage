---
interface Props {
  technologies: {
    name: string;
    icon: string;
  }[];
}

const description =
  "I'm a passionate frontend developer with over 5 years of experience building modern, responsive web applications. I specialize in React, Vue, and TypeScript, and I'm always excited to learn new technologies. When I'm not coding, you'll find me hiking, reading sci-fi novels, or experimenting with new recipes in the kitchen.";

const { technologies } = Astro.props;
---

<section id="about" class="py-20 bg-white">
  <div class="container mx-auto px-4 md:px-6 max-w-prose">
    <p class="text-lg text-gray-700 mb-12 leading-relaxed">
      {description}
    </p>

    <div>
      <h3 class="text-xl font-semibold mb-6 text-center">
        Technologies I Love
      </h3>
      <div
        class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-6 justify-items-center"
      >
        {
          technologies.map((tech) => (
            <div class="flex flex-col items-center group">
              <div class="w-16 h-16 flex items-center justify-center mb-2 bg-gray-100 rounded-lg p-3 transition-transform group-hover:scale-110">
                <img
                  src={tech.icon}
                  alt={tech.name}
                  class="max-w-full max-h-full"
                />
              </div>
              <span class="text-sm font-medium text-gray-700">{tech.name}</span>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
